# Azure subscription ID where the VM is provisioned.
subscription_id = "<your-azure-subscription-id>"
# Used to allow SSH access to the VM from your local machine.
my_ip = "<your-public-ip-cidr>"
# Admin username for the VM
admin_username = "azureuser"